package com.dns_technologies.mlkit_scanner.configs

/** Configurations for the received image from the camera */
class ImageProcessingConfig {
    companion object {
        val resolution = Pair(720, 1280)
    }
}